/**
 * @deprecated Use interop.js — kept as thin re-exports for old import paths.
 */
export {
    notifyMatchSuccess as injectMatchSuccess,
    notifyAddFriend as injectAddFriend,
    notifyFeedRefresh as injectFeedRefresh,
    getInteropMode,
    syncInteropFromSettings,
} from './interop.js';
